import { Open_Sans as OS, Vollkorn as V } from 'next/font/google';

export const OpenSans = OS({
	subsets: ['latin'],
	variable: '--f-open-sans'
});
export const Vollkorn = V({
	subsets: ['latin'],
	variable: '--f-vollkorn'
});
